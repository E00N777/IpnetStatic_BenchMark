#include <stdlib.h>

int main() {
    // 1. 分配一块内存，地址交给 p
    void *p = malloc(100);

    // 2. 这里的逻辑最简单：
    // 直接把 p 变成 NULL (或者指向其他任何非堆地址)。
    // 此时，第一步分配的那 100 字节还在内存里，但已经没有任何变量指向它了。
    // CSA 会在这里立刻报错：指针被覆盖导致泄漏。
    p = NULL; 

    return 0;
}